# TermuxPro 0.9.6-rc.1 候选发布评审记录

日期：2026-09-01

候选状态：`RELEASED`。`dev` 自 `0.9.5` 稳定版后已积累通过门禁的用户可见增值改动，已冻结并发布为
`0.9.6-rc.1` 候选体验版。公开 Pre-release、APK SHA-256、签名证书和 Release run 已完成核验。

## 冻结范围

- PR #192：深色产品页弹窗防线，统一增值页 AlertDialog/列表样式并补静态门禁。
- PR #194：自动研发 PR 合并流程改用 Pulls merge API，避免等待自身检查导致失败/超时噪声。
- PR #195：SSH 目标粘贴输入容错，统一标准化常见 SSH 地址和命令格式。
- PR #196：AI CLI 会话中心显式启动入口，提供 Claude/Codex 新建与历史入口，固定 `ssh_only`。
- PR #197：AI 后 Git 检查入口，从 AI 会话中心直达结构化 Git 工作台概览。

## 增值服务准入

- 分类：远程工作区、AI CLI、Git/tmux 可视化、移动端工作流效率和上下文工具箱。
- 非目标：不研究、不重写 Termux 原始终端、PTY、包管理、本地 shell、基础会话、基础快捷键或基础文件
  能力。
- 风险控制：AI 历史入口不自动恢复最近会话；AI 启动不自动进入 tmux；Git 检查走结构化工作台而非当前
  shell 注入；SSH 粘贴解析只提取目标参数，不执行粘贴命令。

## 候选门禁

- 本地版本、Skill、workflow、GitHub CLI 和 pre-push 冒烟必须通过。
- GitHub 分支 CI 必须通过。
- GitHub Emulator UI 必须通过。
- Release workflow 必须先完成待发布签名 APK 覆盖升级验收，再创建公开 Pre-release。
- 发布后必须核验 APK、`SHA256SUMS` 和 `APK_SIGNATURE.txt` 附件齐全。

## 当前验收记录

- 候选分支：`dev_release096Rc1_20260901`。
- 候选提交：`a9eef66f38b7a69e010d816487dbd105d616f62f`。
- 候选 PR：#198，已合入 `dev`，merge commit `cc9d9f470745e01a24e789581ba315676af213b7`。
- 本地门禁：通过 `termuxpro-version.sh` 版本/标签/Release Notes 检查、`validate-skills.sh`、
  `test/dialog-readable-style-test.sh`、`git diff --check` 和
  `TERMUXPRO_USE_CHINA_MIRROR=1 ./scripts/pre-push-smoke.sh origin/dev`。本轮为 release/docs/test
  证据改动，本地冒烟走静态门禁，不触发本机 Android 重构建。
- 分支 CI：`33577247576`，通过。
- 自动 PR 控制器：`33577247534`，通过。
- dev 收尾 CI：`33577630829`，通过。
- Release workflow：`33578001332`，通过；签名 APK 构建、Release 模拟器覆盖升级验收、公开
  Pre-release 创建和证据上传均完成。
- GitHub Release：<https://github.com/heydarey/TermuxPro/releases/tag/v0.9.6-rc.1>，非草稿
  Pre-release，发布时间 `2026-09-02T01:12:23Z`。
- Release 附件：已核验包含 `termuxpro-0.9.6-rc.1-arm64-v8a.apk`、`SHA256SUMS`、
  `APK_SIGNATURE.txt`。
- APK SHA-256：`3ff940a108b6a6f4d8ae853fa4a4feebcecfa8012841686be9e829647210dc45`。
- 签名证书：`CN=TermuxPro Release, OU=Mobile Development, O=TermuxPro`；
  SHA-256 digest `10c820d392d5bd9672317067a5d11fc1250217824166961df7b385128d4b49cd`。
- 用户通知：候选包可体验后发送一次低频飞书通知，去重键 `release-v0.9.6-rc.1`。

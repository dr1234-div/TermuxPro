# TermuxPro 0.9.0 正式版发布评审记录

## 当前结论

稳定版状态：`READY TO RELEASE`。`0.9.0-rc.3` 已完成候选发布、签名 APK 构建、覆盖升级验收、
Android 运行时冒烟、GitHub Pre-release 创建和资产核验；冻结范围内没有新增 P0/P1 阻断证据。
按发布规则，候选版门禁通过后应在同一维护周期推进 `dev → master → v0.9.0` 稳定版，不继续延期。

## 来源候选版

- 候选标签：`v0.9.0-rc.3`
- 候选 Release：`https://github.com/heydarey/TermuxPro/releases/tag/v0.9.0-rc.3`
- 候选 APK：`termuxpro-0.9.0-rc.3-arm64-v8a.apk`
- 候选 APK SHA-256：`acefb97ad6000d0a5f8dc860bace8ff03984e13dba7ce668c2c73302bce3331d`
- 候选 Release workflow：run `33504695802`，结论 `success`

## 冻结范围

- `0.9.0-rc.1` 已冻结的终端工具箱增值入口范围。
- PR #140：候选 Release 等待增加 Release 页面与附件兜底，减少已发布后仍等待到超时的失败噪声。
- PR #141：AI CLI 启动弹窗补充 Claude 共享账号风险和 Codex 当前项目确认说明。
- PR #143：Release 设备验收脚本允许空设备输出进入明确诊断路径，并补充回归测试。
- PR #145：补录 rc.3 发布证据。

## 非目标

- 不新增 Git/tmux/工作区功能。
- 不解析 Claude/Codex 历史数据。
- 不研究或重做 Termux 原始终端、PTY、包管理、本地 shell、基础会话或基础快捷键能力。

## 稳定版门禁

1. `termuxpro-version.properties` 必须为 `0.9.0` / `90004`，且发布说明文件存在。
2. 稳定版 `versionCode=90004` 必须高于 `0.9.0-rc.3` 的 `90003`。
3. `git diff --check` 必须通过。
4. `./scripts/validate-skills.sh` 必须通过。
5. `./test/version-metadata-test.sh` 必须通过。
6. `./test/workflow-trigger-policy-test.sh` 必须通过。
7. `./test/release-apk-device-smoke-test.sh` 和 `./test/release-abi-gate-test.sh` 必须通过。
8. 稳定准备分支 PR 合入 `dev` 前，分支 CI 必须通过，并等待合入 `dev` 后收尾 CI。
9. `dev → master` 发布 PR 的 CI 必须通过。
10. `v0.9.0` 稳定标签必须指向 `master` 合并提交，Release workflow 必须完成签名 APK 构建、覆盖升级、
    运行时冒烟和 GitHub Release 创建。

## P0/P1 裁定

- rc.3 已修复 rc.2 的发布门禁失败，当前没有启动崩溃、安装失败、无法 SSH 或数据损坏阻断证据。
- 本轮稳定版不新增用户可见功能，只发布候选版已验证范围。
- Termux 原始终端能力不是本轮研发目标，只作为“不受影响”回归面。

## 已完成前置证据

- 候选准备 PR：#144，merge commit `c812ccecd7679c24e2182225d4eed629aa972d13`。
- 候选分支 CI：run `33503904430`，结论 `success`。
- 候选合入 `dev` 后收尾 CI：run `33504302193`，结论 `success`。
- 候选自动 PR：run `33503904226`，结论 `success`。
- 候选 Release workflow：run `33504695802`，结论 `success`。
- 候选证据 PR：#145，merge commit `270c32e8604ae8ad3bd48ea1a101b867695b7f76`。
- 候选证据分支 CI：run `33505481171`，结论 `success`。
- 候选证据自动 PR：run `33505481129`，结论 `success`。
- 候选证据合入 `dev` 后收尾 CI：run `33505528622`，结论 `success`。

## 本地预检证据

- `git diff --check`：通过。
- `./scripts/validate-skills.sh`：通过。
- `./test/version-metadata-test.sh`：通过，确认 `0.9.0 (90004)`。
- `./test/workflow-trigger-policy-test.sh`：通过。
- `./test/release-apk-device-smoke-test.sh`：通过。
- `./test/release-abi-gate-test.sh`：通过。
- `./scripts/pre-push-smoke.sh origin/dev`：通过；本稳定准备分支只更新版本源、发布说明和报告，静态门禁
  覆盖变更范围。

若任一门禁失败，禁止创建稳定标签；必须修复后重新执行受影响门禁。

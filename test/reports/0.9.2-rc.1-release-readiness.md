# TermuxPro 0.9.2-rc.1 候选发布评审记录

## 当前结论

候选版状态：`READY`。`dev` 自 `0.9.1` 稳定版后已合入多项用户可见的 TermuxPro 增值服务改进，并且对应
研发分支 CI、模拟器 UI 和合入 `dev` 后收尾 CI 均已通过。当前冻结为 `0.9.2-rc.1` 候选范围，等待候选
发布分支、Release workflow 和正式签名 APK 产物校验完成。

## 版本元数据

- 候选版本：`0.9.2-rc.1`
- 候选 `versionCode`：`90201`
- 上个稳定版本：`0.9.1`
- 上个稳定 `versionCode`：`90102`
- 发布说明：`docs/RELEASE_NOTES_0.9.2-rc.1.md`

## 冻结范围

- PR #160：终端内 AI 工具箱启动入口，减少远程开发时返回首页的跳转成本。
- PR #161：Claude Code/Codex CLI 启动前展示远端目标，降低误进入错误服务器或目录的风险。
- PR #162：tmux 会话中心优先展示当前工作区会话。
- PR #163：强化增值服务准入边界，明确 Termux 原始功能只作为兼容回归面。
- PR #164：tmux 会话中心补充操作边界提示，非当前工作区会话默认只允许显式进入。
- PR #165：Git 工作台支持按文件查看改动、暂存单文件和取消暂存单文件。

## 增值服务准入裁定

- AI 工具箱启动入口：分类为 `AI CLI` 与 `上下文工具箱`，可进入主线候选范围。
- AI CLI 目标提示：分类为 `AI CLI` 与 `远程工作区`，可进入主线候选范围。
- tmux 会话优先级与安全提示：分类为 `tmux/Git 可视化` 与 `远程工作区`，可进入主线候选范围。
- Git 文件级暂存管理：分类为 `tmux/Git 可视化` 与 `移动端工作流效率`，可进入主线候选范围。
- Termux 原始终端、PTY、包管理、本地 shell、基础会话和基础快捷键：不作为本轮新增能力，只作为回归保护。

## 已通过证据

- PR #160：merge commit `2aeae021`；分支 CI run `33524618827`，模拟器 UI run `33524618837`，自动 PR run
  `33524618916`，dev 收尾 CI run `33525348234`，结论均为 `success`。
- PR #161：merge commit `64ab9c81`；分支 CI run `33526205104`，模拟器 UI run `33526205098`，自动 PR run
  `33526205119`，dev 收尾 CI run `33526838339`，结论均为 `success`。
- PR #162：merge commit `adb2f0d2`；分支 CI run `33527734015`，模拟器 UI run `33527733906`，自动 PR run
  `33527733854`，dev 收尾 CI run `33528372891`，结论均为 `success`。
- PR #163：merge commit `1d647746`；分支 CI run `33528819412`，自动 PR run `33528819476`，dev 收尾 CI
  run `33528873527`，结论均为 `success`。
- PR #164：merge commit `f6e55fcf`；分支 CI run `33529233414`，模拟器 UI run `33529233507`，自动 PR run
  `33529233394`，dev 收尾 CI run `33529823713`，结论均为 `success`。
- PR #165：merge commit `ebc1807e`；分支 CI run `33531685536`，模拟器 UI run `33531685590`，自动 PR run
  `33531685548`，dev 收尾 CI run `33532295996`，结论均为 `success`。

## 候选发布门禁

1. `termuxpro-version.properties` 必须为 `0.9.2-rc.1` / `90201`，且发布说明文件存在。
2. `versionCode=90201` 必须高于 `0.9.1` 稳定版 `90102`。
3. `git diff --check`、`./test/pre-push-smoke-test.sh` 和 `./scripts/pre-push-smoke.sh origin/dev` 必须通过。
4. 候选发布分支合入 `dev` 前，分支 CI、适用模拟器 UI 和自动 PR 工作流必须通过。
5. 候选合入 `dev` 后，收尾 CI 必须通过。
6. `v0.9.2-rc.1` 标签必须由候选发布自动化从唯一版本源创建，Release workflow 必须完成签名 APK 构建、
   覆盖升级、运行时冒烟和 GitHub Pre-release 创建。
7. 候选 Release 必须包含 APK、`SHA256SUMS` 和 `APK_SIGNATURE.txt`，并记录 APK SHA-256。

## P0/P1 裁定

- 当前冻结范围没有已知启动崩溃、安装失败、无法 SSH、误杀真实 tmux、数据损坏或 Release 产物不可安装
  证据。
- Git 文件级操作只修改暂存区，不执行提交、推送、reset 或工作区丢弃，风险边界可控。
- 原始 Termux 能力不是本轮主线研发对象；后续测试只验证 TermuxPro 增值层没有影响原始能力。

## 待发布后补录

- 候选准备 PR、merge commit、分支 CI 与 dev 收尾 CI。
- `v0.9.2-rc.1` Release workflow run。
- 候选 Release URL、APK 文件名、大小、SHA-256 和签名证书指纹。

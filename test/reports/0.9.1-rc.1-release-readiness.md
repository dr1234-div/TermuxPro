# TermuxPro 0.9.1-rc.1 候选发布评审记录

## 当前结论

候选状态：`READY_FOR_RC_PR`。`dev` 自 `0.9.0` 稳定版后已有用户可见增值服务改动，且最近研发 PR、模拟器
UI、`dev` 合并后 CI 均已通过。按发布规则冻结为 `0.9.1-rc.1`，不继续把已完成体验修复无限堆在 `dev`。

## 冻结范围

- PR #149：自动 PR 单一控制器规则，减少重复控制器、pending 检查和取消/失败邮件噪声。
- PR #150：tmux 会话归属提示细分，非当前工作区会话只允许显式进入，禁止越权重命名/停止。
- PR #151：AI CLI 启动弹窗展示远端目标和工具差异提示，避免共享 Claude 账号或多项目误入。
- PR #152：Git 工作台首屏展示 `host:port · path`，多服务器场景下先确认目标再操作 Git。
- PR #153：复核 GitHub Actions 失败通知噪声，区分真实 failure、并发取消和替代成功 run。

## 候选门禁

1. `termuxpro-version.properties` 必须为 `0.9.1-rc.1` / `90101`，发布说明文件存在。
2. `versionCode=90101` 必须高于 `0.9.0` 稳定版 `90004`。
3. `./scripts/codex-quota-guard.sh` 必须允许继续。
4. `./scripts/resource-guard.sh` 必须通过，并保持共享服务器低并发策略。
5. `git diff --check` 和 `./scripts/validate-skills.sh` 必须通过。
6. 版本、workflow、GitHub CLI 封装和预推送冒烟静态门禁必须通过。
7. `dev_release091Rc1_20260901 → dev` PR 的 CI 与 Emulator UI 必须通过，并等待合入 `dev` 后收尾 CI。
8. 候选标签 `v0.9.1-rc.1` 必须由自动 PR 流水线在合并提交上创建，不覆盖既有标签。
9. Release workflow 必须完成签名 APK 构建、覆盖升级、运行时冒烟和 GitHub Release 创建。

## P0/P1 裁定

- 本候选版只冻结 TermuxPro 增值服务：远程 AI CLI、tmux/Git 可视化管理和发布治理。
- 不研究、不重做 Termux 原始终端、PTY、包管理、本地 shell、基础会话或基础快捷键。
- 原始终端能力的验收重点是“不受影响”；候选变更失败时必须安全回退到原始终端可用状态。

## 本地预检证据

- `./scripts/codex-quota-guard.sh`：总剩余额度 56%，允许继续。
- `./scripts/resource-guard.sh`：远程/共享环境资源守卫通过；KVM 不可用，未启动本地模拟器。
- 最近 `dev` 收尾 CI：run `33515304640`，结论 `success`。
- 最近 20 条工作流：均为 `success`，无打开 PR。

## 发布后补录

待候选 PR、CI、Release workflow 和 APK SHA-256 完成后补录。

# TermuxPro 0.9.3 稳定发布评审记录

## 当前结论

稳定版状态：`RELEASED`。`0.9.3` 是 `0.9.2` 稳定标签发布失败后的发布门禁修复版，应用用户功能沿用
`0.9.2-rc.1` 已验证范围，不加入新的未验证功能。hotfix 分支、`dev → master` 发布 PR、master CI、
稳定标签、Release workflow、覆盖升级验收和公开稳定 GitHub Release 均已完成。

## 来源候选版与失败版本

- 来源候选标签：`v0.9.2-rc.1`
- 来源候选 Release：`https://github.com/heydarey/TermuxPro/releases/tag/v0.9.2-rc.1`
- 来源候选 APK SHA-256：`307a2dccfa95019d847e3a4751a900aea8cb49aa419bb600e8f49a9d44e4600d`
- 失败稳定标签：`v0.9.2`
- 失败 Release workflow：run `33538417112`，结论 `failure`
- 失败根因：覆盖升级验收脚本启动等待假阴性；公开稳定 Release 未创建。

## 版本元数据

- 稳定版本：`0.9.3`
- 稳定 `versionCode`：`90302`
- 发布说明：`docs/RELEASE_NOTES_0.9.3.md`

## 冻结范围

- 用户功能范围沿用 `0.9.2-rc.1`：AI CLI 终端入口/目标提示、tmux 当前工作区优先展示与操作边界提示、
  Git 单文件暂存管理。
- 发布门禁修复：`scripts/verify-release-apk-on-device.sh` 显式启动 Launcher Activity，等待进程稳定态，
  并保留完整失败证据。

## 稳定版门禁

1. `termuxpro-version.properties` 必须为 `0.9.3` / `90302`，且发布说明文件存在。
2. `versionCode=90302` 必须高于 `0.9.2` 的 `90202` 与 `0.9.2-rc.1` 的 `90201`。
3. `git diff --check`、`./scripts/validate-skills.sh`、`./test/version-metadata-test.sh`、
   `./test/workflow-trigger-policy-test.sh`、`./test/release-apk-device-smoke-test.sh` 和
   `./test/release-abi-gate-test.sh` 必须通过。
4. hotfix 分支 PR 合入 `dev` 前，分支 CI 与适用门禁必须通过，并等待合入 `dev` 后收尾 CI。
5. `dev → master` 发布 PR 的 CI 必须通过。
6. `v0.9.3` 稳定标签必须指向 `master` 合并提交，Release workflow 必须完成签名 APK 构建、覆盖升级、
   运行时冒烟和 GitHub Release 创建。
7. 稳定 Release 必须是非草稿、非预发布，附件包含 APK、`SHA256SUMS` 和 `APK_SIGNATURE.txt`。
8. 稳定 Release 完成后必须将 `dev` 对齐 `master` 并等待收尾 CI。

## P0/P1 裁定

- 当前没有应用启动崩溃、安装失败、无法 SSH、误杀 tmux、数据损坏或 Release 产物不可安装证据。
- `0.9.2` 失败属于发布验收脚本假阴性；修复后必须用同一 Release workflow 重新证明。
- 原始 Termux 能力不是本轮主线研发对象；只作为增值层不破坏的兼容回归面。

## 稳定发布证据

- hotfix PR：#170，merge commit `c51658c0b4d46d6f3d6a773478bc8d9f4f2f2fc2`。
- hotfix 分支 CI：run `33539770613`，结论 `success`。
- hotfix 自动 PR：run `33539770734`，结论 `success`。
- hotfix 合入 `dev` 后收尾 CI：run `33540257740`，结论 `success`。
- 发布 PR：#171，`dev → master`，merge commit `12803938fc69921af1063430545e9b3d9aa5875b`。
- 发布 PR CI：run `33540993969`，结论 `success`。
- master 合入后 CI：run `33541563903`，结论 `success`。
- 稳定标签：`v0.9.3`，指向 `12803938fc69921af1063430545e9b3d9aa5875b`。
- 稳定 Release workflow：run `33542158844`，结论 `success`。
- 稳定 Release：`https://github.com/heydarey/TermuxPro/releases/tag/v0.9.3`。
- Release 状态：非 Draft、非 Pre-release。
- 发布资产：`termuxpro-0.9.3-arm64-v8a.apk`、`SHA256SUMS`、`APK_SIGNATURE.txt`。
- 正式 APK 大小：`33883405` bytes。
- 正式 APK SHA-256：`0cee528ce0579ac73f75d56c60e5c2c48f0c869697d8ed8a518ce1b97515b32b`。
- 签名证书 SHA-256：`10c820d392d5bd9672317067a5d11fc1250217824166961df7b385128d4b49cd`。
- APK 签名校验：v1 `true`，v2 `true`，签名者数量 `1`。
- 稳定 Release 完成后已将 `dev` 快进到 `master` 发布提交，并触发 dev 对齐 CI run `33543016211`。

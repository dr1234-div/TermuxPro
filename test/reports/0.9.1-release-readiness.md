# TermuxPro 0.9.1 稳定发布评审记录

## 当前结论

稳定版状态：`RELEASED`。`0.9.1-rc.1` 已完成候选发布、签名 APK 构建、模拟器覆盖升级验收和
GitHub Release 创建；冻结范围内没有新增 P0/P1 阻断证据。`0.9.1` 稳定版沿用候选范围，不加入新功能，
并已完成 `dev → master` 发布 PR、master CI、稳定标签、Release workflow、签名 APK 覆盖升级验收和
公开 GitHub Release 创建。

## 来源候选版

- 候选标签：`v0.9.1-rc.1`
- 候选 Release：`https://github.com/heydarey/TermuxPro/releases/tag/v0.9.1-rc.1`
- 候选 APK：`termuxpro-0.9.1-rc.1-arm64-v8a.apk`
- 候选 APK SHA-256：`ee34b660a8b448d796864413ccdd6bc4188ac8179914b018e9d2d1b125fba5d3`
- 候选 Release workflow：run `33516755674`，结论 `success`

## 冻结范围

- PR #149：自动 PR 单一控制器规则，减少重复控制器和取消/失败邮件噪声。
- PR #150：tmux 会话归属提示细分，非当前工作区会话只允许显式进入。
- PR #151：AI CLI 启动弹窗展示远端目标和 Claude/Codex 差异提示。
- PR #152：Git 工作台首屏展示 `host:port · path`。
- PR #153：GitHub Actions 失败通知噪声复核。
- PR #154：`0.9.1-rc.1` 候选发布。
- PR #155：补录 `0.9.1-rc.1` 候选发布证据。
- PR #158：修复发布 PR 的 CI 打包 OOM 风险，将 Lint 与 Debug APK 拆为独立 Gradle 进程，并为
  GitHub Runner 的 APK 打包阶段单独提高堆内存；该改动只影响 CI 可靠性，不改变应用运行行为。

## 稳定版门禁

1. `termuxpro-version.properties` 必须为 `0.9.1` / `90102`，且发布说明文件存在。
2. 稳定版 `versionCode=90102` 必须高于 `0.9.1-rc.1` 的 `90101`。
3. `git diff --check` 和 `./scripts/validate-skills.sh` 必须通过。
4. `./test/version-metadata-test.sh`、`./test/workflow-trigger-policy-test.sh`、`./test/release-apk-device-smoke-test.sh`
   和 `./test/release-abi-gate-test.sh` 必须通过。
5. 稳定准备分支 PR 合入 `dev` 前，分支 CI 与适用门禁必须通过，并等待合入 `dev` 后收尾 CI。
6. `dev → master` 发布 PR 的 CI 必须通过。
7. `v0.9.1` 稳定标签必须指向 `master` 合并提交，Release workflow 必须完成签名 APK 构建、覆盖升级、
   运行时冒烟和 GitHub Release 创建。
8. 稳定 Release 必须是非草稿、非预发布，附件包含 APK、`SHA256SUMS` 和 `APK_SIGNATURE.txt`。
9. 稳定 Release 完成后必须将 `dev` 对齐 `master` 并等待收尾 CI。

## P0/P1 裁定

- 本轮稳定版只发布候选版已验证范围，不新增 SSH、tmux、Git 或 AI CLI 行为。
- 候选范围属于 TermuxPro 增值服务；Termux 原始终端能力只作为不受影响回归面。
- 当前没有启动崩溃、安装失败、无法 SSH、误杀 tmux、数据损坏或 Release 产物不可安装证据。

## 已完成前置证据

- 候选准备 PR：#154，merge commit `4e1395445a144947aa336cf5434cb5f57de480dc`。
- 候选分支 CI：run `33515746823`，结论 `success`。
- 候选自动 PR：run `33515746920`，结论 `success`。
- 候选合入 `dev` 后收尾 CI：run `33516217907`，结论 `success`。
- 候选 Release workflow：run `33516755674`，结论 `success`。
- 候选证据补录 PR：#155，merge commit `7ed2afa9cec8fcf1807fdead30674f1ba8f52918`。
- 候选证据补录收尾 CI：run `33517838042`，结论 `success`。

## 稳定发布证据

- 稳定准备 PR：#156，merge commit `e9e21d9788adcd071653e03dbcf259138f586d9d`。
- 稳定准备分支 CI：run `33518177025`，结论 `success`。
- 稳定准备自动 PR：run `33518177302`，结论 `success`。
- 稳定准备合入 `dev` 后收尾 CI：run `33518725137`，结论 `success`。
- 发布 PR：#157，`dev → master`，merge commit `3f8613b91530b0c210c016727bd4d6cfd3baf22b`。
- 发布 PR 首轮 CI：run `33519290843` 在 `Lint 与 Debug APK` 因 GitHub Runner Java heap OOM 失败，
  未合入 master、未创建稳定标签。
- CI OOM 修复 PR：#158，merge commit `413f2b95e99cfad7f4e7e4067afb765147e9a6c2`。
- CI OOM 修复分支 CI：run `33520018213`，结论 `success`，`Lint 与 Debug APK` 通过。
- CI OOM 修复合入 `dev` 后收尾 CI：run `33520634217`，结论 `success`，`Lint 与 Debug APK` 通过。
- 发布 PR 最新 head `413f2b95e99cfad7f4e7e4067afb765147e9a6c2` 的 PR run 初始出现
  `action_required` 且无 jobs；已通过 GitHub API 批准后生成 jobs，该状态属于 GitHub 对 PR workflow
  执行的安全确认，不是代码失败。
- 发布 PR 最新 CI：run `33520816371`，结论 `success`。
- 发布 PR 最新模拟器 UI：run `33520816333`，结论 `success`。
- master 合入后 CI：run `33521956638`，结论 `success`。
- 稳定标签：`v0.9.1`，指向 `3f8613b91530b0c210c016727bd4d6cfd3baf22b`。
- 稳定 Release workflow：run `33522531577`，结论 `success`。
- 稳定 Release：`https://github.com/heydarey/TermuxPro/releases/tag/v0.9.1`。
- Release 状态：非 Draft、非 Pre-release。
- 发布资产：`termuxpro-0.9.1-arm64-v8a.apk`、`SHA256SUMS`、`APK_SIGNATURE.txt`。
- 正式 APK 大小：`33870248` bytes。
- 正式 APK SHA-256：`ff0de6b1471ba01dcb6273aba6881862b0e67bad016d6852aeab5278b198d4c4`。
- 签名证书 SHA-256：`10c820d392d5bd9672317067a5d11fc1250217824166961df7b385128d4b49cd`。
- APK 签名校验：v1 `true`，v2 `true`，签名者数量 `1`。

# TermuxPro 0.9.5 稳定发布评审记录

日期：2026-09-01

稳定版状态：`READY`。`0.9.5-rc.1` 已完成候选 Release、签名 APK 构建、覆盖升级验收、附件核验和
低频用户通知；当前没有发现冻结范围内的 P0/P1 阻断证据。本轮按发布规则推进 `dev → master`、
`v0.9.5` 稳定标签、正式 Release workflow、附件核验和 `dev` 对齐收尾 CI。

## 来源候选版

- 来源候选标签：`v0.9.5-rc.1`
- 来源候选 Release：`https://github.com/heydarey/TermuxPro/releases/tag/v0.9.5-rc.1`
- 来源候选 APK SHA-256：
  `27cb49d5a211df77cb3c484f19e5cc90dfd4589bc11f4e44cb9edb94ea21302b`
- 来源候选 Release workflow：run `33566239673`，结论 `success`

## 版本元数据

- 稳定版本：`0.9.5`
- 稳定 `versionCode`：`90502`
- 发布说明：`docs/RELEASE_NOTES_0.9.5.md`

## 冻结范围

- PR #183：AI CLI 会话中心，集中展示当前工作区、Claude/Codex 新建与历史选择命令预览和共享账号风险。
- PR #184：Git Stash 工作台，支持保存、查看、应用和删除指定 stash，不提交、不推送、不清空全部 stash。
- PR #185：Git 分支切换安全门禁，脏工作区阻止本地分支切换和远端跟踪分支创建。
- PR #186：Claude 快捷指令模板防误续接，移除 `claude --continue`，区分新建独立会话和历史选择。
- PR #187、#188：`0.9.5-rc.1` 候选发布和候选发布证据补录。

## 增值服务准入

- 分类：AI CLI、自定义快捷指令、Git 可视化、远程工作区、移动端工作流效率和上下文工具箱。
- 非目标：不研究、不重写 Termux 原始终端、PTY、包管理、本地 shell、基础会话、基础快捷键或基础文件
  能力。原始 Termux 能力只做兼容守护和“不受影响”回归保护。
- 风险控制：AI 历史入口不自动恢复最近会话；Git 写操作都有显式确认和脏工作区门禁；Stash 删除只作用
  于用户选择的 `stash@{N}`；稳定版不加入候选版之外的新功能。

## 稳定版门禁

1. `termuxpro-version.properties` 必须为 `0.9.5` / `90502`，且发布说明文件存在。
2. `versionCode=90502` 必须高于 `0.9.5-rc.1` 的 `90501`。
3. `git diff --check`、`./scripts/validate-skills.sh`、`./test/version-metadata-test.sh`、
   `./test/workflow-trigger-policy-test.sh`、`./test/release-apk-device-smoke-test.sh` 和
   `./test/release-abi-gate-test.sh` 必须通过。
4. 稳定准备分支合入 `dev` 前，分支 CI 与适用门禁必须通过，并等待合入 `dev` 后收尾 CI。
5. `dev → master` 发布 PR 的 CI 必须通过。
6. `v0.9.5` 稳定标签必须指向 `master` 合并提交，Release workflow 必须完成签名 APK 构建、覆盖升级、
   运行时冒烟和 GitHub Release 创建。
7. 稳定 Release 必须是非草稿、非预发布，附件包含 APK、`SHA256SUMS` 和 `APK_SIGNATURE.txt`。
8. 稳定 Release 完成后必须将 `dev` 对齐 `master` 并等待收尾 CI。

## P0/P1 裁定

- 当前没有应用启动崩溃、安装失败、无法 SSH、误杀 tmux、数据损坏或 Release 产物不可安装证据。
- 本轮不把 Termux 原始能力作为产品主线；只确认 TermuxPro 增值层没有修改其基础语义。
- `0.9.5-rc.1` 已完成候选发布门禁，未发现阻止同周期推进稳定版的 P0/P1。

## 稳定发布证据

- 稳定准备分支：`dev_stable095_20260901`。
- 稳定准备提交：待提交后回填。
- 稳定准备 PR：待自动研发 PR 创建并合入后回填。
- 稳定准备本地门禁：待执行后回填。
- 稳定准备分支 CI：待 GitHub Actions 完成后回填。
- 稳定准备自动 PR：待 GitHub Actions 完成后回填。
- 稳定准备合入 `dev` 后收尾 CI：待自动 PR 合入后回填。
- 发布 PR：待创建 `dev → master` PR 后回填。
- 发布 PR CI：待 GitHub Actions 完成后回填。
- 发布 PR Emulator UI：待 GitHub Actions 完成后回填。
- master 合入后 CI：待发布 PR 合入后回填。
- 稳定标签：待 `v0.9.5` 创建后回填。
- 稳定 Release workflow：待标签触发并完成后回填。
- 稳定 Release：待发布后回填。
- Release 状态：待发布后回填；必须为非 Draft、非 Pre-release。
- 发布资产：待发布后回填；必须包含 `termuxpro-0.9.5-arm64-v8a.apk`、`SHA256SUMS` 和
  `APK_SIGNATURE.txt`。
- 正式 APK SHA-256：待发布后回填。
- 签名证书 SHA-256：待发布后回填。
- APK 签名校验：待发布后回填。
- 稳定 Release 完成后 dev 对齐 CI：待完成后回填。
- 用户通知：按低频通知规则，仅在稳定包可安装后发送一次飞书通知，去重键 `release-v0.9.5`。

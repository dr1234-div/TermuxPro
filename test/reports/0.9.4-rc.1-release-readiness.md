# TermuxPro 0.9.4-rc.1 候选发布评审记录

日期：2026-09-01

候选状态：`RELEASED`。`dev` 自 `0.9.3` 稳定版后已有通过门禁的用户可见增值改动，已冻结并发布为
`0.9.4-rc.1` 候选体验版。

## 冻结范围

- PR #173：明确增值服务边界，Termux 原始能力只做兼容守护和“不受影响”回归。
- PR #174、#175：快捷指令模板引导与验收证据。
- PR #176、#177：AI CLI 启动上下文防误入与验收证据。

## 增值服务准入

- 分类：自定义快捷指令、AI CLI、远程工作区、移动端工作流效率、上下文工具箱和发布流程稳定性。
- 非目标：不研究、不重写 Termux 原始终端、PTY、包管理、本地 shell、基础会话、基础快捷键或基础文件
  能力。
- 风险控制：模板不自动执行；AI 历史入口不自动恢复最近会话；发布流程不覆盖、不移动既有标签。

## 候选门禁

- 本地版本、Skill、workflow、GitHub CLI 和 pre-push 冒烟必须通过。
- GitHub 分支 CI 必须通过。
- GitHub Emulator UI 必须通过。
- Release workflow 必须先完成待发布签名 APK 覆盖升级验收，再创建公开 Pre-release。
- 发布后必须核验 APK、`SHA256SUMS` 和 `APK_SIGNATURE.txt` 附件齐全。

## 当前验收记录

- 候选分支：`dev_release094Rc1_20260901`。
- 候选提交：`7bf4b91a69a96ef194b45ab486983a874c8c7b8d`。
- 候选 PR：#178，已合入 `dev`，merge commit
  `34a7f4b23eadd86d5717917195169f0fcf37a361`。
- 本地门禁：资源守卫、`git diff --check`、Skill 校验、版本元数据、workflow 策略、GitHub CLI 封装、
  check suite 诊断脚本、Release APK 设备冒烟脚本、Release ABI 门禁、pre-push 冒烟映射和正式
  `scripts/pre-push-smoke.sh origin/dev` 均通过。
- 分支 CI：run `33551347585`，`TermuxPro CI`，结论 `success`。
- 自动 PR 控制器：run `33551347733`，结论 `success`；已完成 PR 创建、合并、dev 收尾 CI 等待和候选
  Release 调度。
- dev 收尾 CI：run `33551828340`，merge commit
  `34a7f4b23eadd86d5717917195169f0fcf37a361`，结论 `success`。
- Release workflow：run `33552386533`，结论 `success`；签名 APK 构建、待发布 APK 覆盖升级验收和公开
  Pre-release 创建均通过。
- GitHub Release：`https://github.com/heydarey/TermuxPro/releases/tag/v0.9.4-rc.1`，非草稿，
  Pre-release。
- Release 附件：`termuxpro-0.9.4-rc.1-arm64-v8a.apk`、`SHA256SUMS`、`APK_SIGNATURE.txt` 齐全。
- APK SHA-256：
  `a978b62d0285bfd463a7af8e5c108d324fb7bfa146b725b3c06e964c3070e788`。
- 签名证据：`APK_SIGNATURE.txt` 显示 v1/v2 签名校验通过，证书 SHA-256 digest 为
  `10c820d392d5bd9672317067a5d11fc1250217824166961df7b385128d4b49cd`。
- 用户通知：按低频通知规则，仅在候选包可体验后发送一次飞书通知，去重键
  `release-v0.9.4-rc.1`。

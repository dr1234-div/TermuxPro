# TermuxPro 0.6.0 正式版发布评审记录

## 当前结论

正式版状态：`RELEASED`。`0.6.0` 已通过 `dev → master` 发布 PR、稳定标签 Release workflow、
正式签名 APK 覆盖升级验收和发布后 `dev` 同步收尾 CI。当前冻结范围没有未关闭 P0/P1 阻断，
允许用户下载 `v0.6.0` 正式包体验；后续普通迭代从已对齐 `master` 的 `dev` 继续。

## 冻结范围

- PR #69：支持从远端跟踪分支创建本地跟踪分支并切换。
- PR #70：统一 Git 工作台弹窗可读性。
- PR #72：提升多工作区入口可发现性，单工作区时不展示伪选择器。
- PR #73：支持新建本地分支并切换。
- PR #74：Git 概览拆分已暂存/未暂存状态，并支持暂存全部/取消暂存全部。
- PR #76：修复 Release 模拟器验收 ABI 不匹配；公开 APK 保持 arm64，模拟器门禁使用同签名 x86_64 APK。
- PR #78：补齐干净 worktree 构建基线验收 APK 时缺少 `:app:downloadBootstraps` 的发布门禁缺陷。
- PR #80：将 `0.5.0` 终端触摸滚动缺陷状态更新为 `0.5.1+` 已修复并保留回归条件。
- PR #81：发布门禁运行时证据不再允许空文件静默通过。

冻结后不再并入新的 P2 或普通功能；门禁失败只修复当前发布范围并重新评审，不覆盖既有候选或稳定标签。

## 候选证据

- 标签 `v0.6.0-rc.3` 指向 `3155236989a3d6a71685e51d23f8dbe9886b78e7`。
- Release run `33418901155` 成功，GitHub Release `v0.6.0-rc.3` 非 Draft、标记为 Pre-release。
- 候选 APK：`termuxpro-0.6.0-rc.3-arm64-v8a.apk`，大小 `33816194` 字节。
- 候选 APK SHA-256：
  `a9c78a2f8c04b83c79f323769f2ffa21a40d79177dad9b58e6e471a8a1f1aa80`。
- 候选发布覆盖升级门禁：`install-baseline.txt` 与 `install-candidate.txt` 均为 `Success`，
  `result.txt` 记录 `baselineUid=10209`、`candidateUid=10209`、`versionName=0.6.0-rc.3`、
  `versionCode=60003`。
- 已知候选证据缺口：`0.6.0-rc.3` 的 `android-runtime.txt` 为空；PR #81 已修复后续发布门禁，
  稳定版发布必须重新生成非空运行时证据。

## 稳定版前置证据

- 当前稳定版元数据：`versionName=0.6.0`、`versionCode=60004`、
  `releaseNotes=docs/RELEASE_NOTES_0.6.0.md`。
- `versionCode=60004` 高于 `0.6.0-rc.3` 的 `60003`。
- PR #81 合入 `dev` 后，merge commit `76947767f79d54b087d51a34925907489733dd55` 的 CI run
  `33421756059` 成功。
- GitHub check suite 诊断中外部 App `vercel`、`4everland`、`cursor` 的 queued 0 job 空检查未被设置为
  `master/dev` 必需状态检查，不阻断当前发布；仓库自身 GitHub Actions 必须以最新 dev CI 结果为准。

## 稳定版发布证据

- 发布 PR：#83 `build(release): 发布零点六正式版本`，`dev → master`，合并时间
  `2026-08-31T18:15:43Z`，master merge commit
  `f3c9c84ab550d26e1ef6a5e5e20fe9f69cb7e8ca`。
- 发布标签：`v0.6.0`，指向 `master` 发布提交 `f3c9c84ab550d26e1ef6a5e5e20fe9f69cb7e8ca`。
- Release run：`33424068630`，结论 `success`。
- GitHub Release：`https://github.com/heydarey/TermuxPro/releases/tag/v0.6.0`，
  非 Draft，非 Pre-release，发布时间 `2026-08-31T18:23:25Z`。
- 正式 APK：`termuxpro-0.6.0-arm64-v8a.apk`，大小 `33816188` 字节。
- 正式 APK SHA-256：
  `600492d4da39e928c31414df34a44d45685202aabb84bffb0adbcf8e0ba2e2d7`。
- APK 签名报告：Release 资产 `APK_SIGNATURE.txt`，资产 digest
  `sha256:68192c6503982bf22ab4a0329db2aa8f18892044c43832f5da73d104464e3e7d`。
- 发布设备门禁：`install-baseline.txt` 与 `install-candidate.txt` 均为 `Success`；
  `result.txt` 记录 `baselineUid=10209`、`candidateUid=10209`、`versionName=0.6.0`、
  `versionCode=60004`。
- 发布运行时证据：Google `sdk_gphone64_x86_64`，Android 15 / API 35，
  supported ABIs `x86_64,arm64-v8a`，默认输入法 LatinIME，`processPid=4430`，
  `androidRuntimeFatalException=false`，`androidRuntimeLog=EMPTY_NO_FATAL_EXCEPTION`。
- 分支同步：稳定 Release 后 `dev` 已快进到 `master` 发布提交
  `f3c9c84ab550d26e1ef6a5e5e20fe9f69cb7e8ca`；同步 CI run `33424826518` 结论 `success`。

## P0/P1 裁定

- 当前全局 Backlog 中仍保留 SSH、tmux、Claude Code 共享账号隔离、AI 会话中心和真机验收等长期质量项；
  这些属于持续产品主线，不在本次 0.6.0 冻结范围内直接新增风险。
- 本次冻结范围内的用户可见变更集中在 Git 工作台、工作区入口和发布门禁。对应功能已经通过本地测试、
  GitHub CI、模拟器默认/200% 字体截图和候选发布门禁；未发现阻断稳定版的 P0/P1。
- 若稳定发布流程或正式 APK 体验发现启动、覆盖升级、SSH 主路径、触摸滚动、Git index 安全或深色文字
  不可读回归，立即停止稳定发布并重新进入候选修复流程。

## 稳定版发布门禁

1. 本地 `TERMUXPRO_USE_CHINA_MIRROR=1 ./scripts/test-all.sh && git diff --check` 必须通过。
2. 稳定版元数据 PR 的完整 CI 与 Emulator UI 必须通过。
3. 稳定版元数据 PR 合入 `dev` 后，merge commit 收尾 CI 必须通过。
4. 创建 `dev → master` 发布 PR；发布 PR 的完整 CI 与 Emulator UI 必须通过。
5. 发布 PR 合入 `master` 后，在 `master` 合并提交创建不可覆盖标签 `v0.6.0`。
6. Release 工作流必须先完成正式签名 APK 覆盖升级验收，再创建 GitHub Stable Release。
7. Release 必须非 Draft、非 Pre-release，并包含 APK、`SHA256SUMS` 和 `APK_SIGNATURE.txt`。
8. 稳定 Release 的 `android-runtime.txt` 证据必须非空，并记录设备/系统/ABI/输入法/进程/无崩溃结论。
9. 稳定 Release 完成后，`dev` 必须对齐 `master` 发布提交并等待收尾 CI 成功。

## 发布结论

`v0.6.0` 已完成稳定发布。后续不得覆盖或移动 `v0.6.0` 标签；若用户在正式包中复现启动、覆盖升级、
SSH 主路径、触摸滚动、Git index 安全或深色文字不可读回归，应进入 `0.6.1` 候选/补丁流程并保留失败证据。

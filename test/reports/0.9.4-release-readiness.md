# TermuxPro 0.9.4 稳定发布评审记录

日期：2026-09-01

稳定版状态：`READY`。`0.9.4-rc.1` 已完成候选 Release、签名 APK 构建、覆盖升级验收、附件核验和
低频用户通知；当前没有发现冻结范围内的 P0/P1 阻断证据。按发布规则，本轮应推进 `dev → master`
并发布 `v0.9.4` 稳定版，不继续把新功能塞入当前发布列车。

## 来源候选版

- 来源候选标签：`v0.9.4-rc.1`
- 来源候选 Release：`https://github.com/heydarey/TermuxPro/releases/tag/v0.9.4-rc.1`
- 来源候选 APK SHA-256：
  `a978b62d0285bfd463a7af8e5c108d324fb7bfa146b725b3c06e964c3070e788`
- 来源候选 Release workflow：run `33552386533`，结论 `success`

## 版本元数据

- 稳定版本：`0.9.4`
- 稳定 `versionCode`：`90402`
- 发布说明：`docs/RELEASE_NOTES_0.9.4.md`

## 冻结范围

- PR #173：明确增值服务边界，Termux 原始能力只做兼容守护和“不受影响”回归。
- PR #174、#175：快捷指令模板引导与验收证据。
- PR #176、#177：AI CLI 启动上下文防误入与验收证据。
- PR #178、#179：`0.9.4-rc.1` 候选发布与候选证据补录。

## 增值服务准入

- 分类：自定义快捷指令、AI CLI、远程工作区、移动端工作流效率、上下文工具箱和发布流程稳定性。
- 非目标：不研究、不重写 Termux 原始终端、PTY、包管理、本地 shell、基础会话、基础快捷键或基础文件
  能力。原始 Termux 能力只做兼容守护和“不受影响”回归保护。
- 风险控制：模板不自动执行；AI 历史入口不自动恢复最近会话；稳定版不加入候选版之外的新功能。

## 稳定版门禁

1. `termuxpro-version.properties` 必须为 `0.9.4` / `90402`，且发布说明文件存在。
2. `versionCode=90402` 必须高于 `0.9.4-rc.1` 的 `90401`。
3. `git diff --check`、`./scripts/validate-skills.sh`、`./test/version-metadata-test.sh`、
   `./test/workflow-trigger-policy-test.sh`、`./test/release-apk-device-smoke-test.sh` 和
   `./test/release-abi-gate-test.sh` 必须通过。
4. 稳定准备分支合入 `dev` 前，分支 CI 与适用门禁必须通过，并等待合入 `dev` 后收尾 CI。
5. `dev → master` 发布 PR 的 CI 必须通过。
6. `v0.9.4` 稳定标签必须指向 `master` 合并提交，Release workflow 必须完成签名 APK 构建、覆盖升级、
   运行时冒烟和 GitHub Release 创建。
7. 稳定 Release 必须是非草稿、非预发布，附件包含 APK、`SHA256SUMS` 和 `APK_SIGNATURE.txt`。
8. 稳定 Release 完成后必须将 `dev` 对齐 `master` 并等待收尾 CI。

## P0/P1 裁定

- 当前没有应用启动崩溃、安装失败、无法 SSH、误杀 tmux、数据损坏或 Release 产物不可安装证据。
- 当前唯一 open issue 是迭代巡检，不是 P0/P1 发布阻断项。
- 本轮不把 Termux 原始能力作为产品主线；只确认 TermuxPro 增值层没有修改其基础语义。

## 稳定发布证据

- 待稳定准备分支、发布 PR、master CI、稳定标签、Release workflow、APK SHA-256 和签名证据完成后补录。

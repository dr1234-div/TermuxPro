# TermuxPro 0.2.0 正式发布验收记录

## 结论

状态：`READY`。`0.2.0-rc.2` 的发布产物与最新 UI 回归通过；正式版只包含发布规则固化、AI 会话弹窗
200% 字体换行修复和版本提升，不引入新的业务范围。

## 可核验证据

- 候选 Release：`v0.2.0-rc.2`，Release 工作流 `33303274098` 通过，APK、SHA-256 与签名摘要完整。
- 最新模拟器 UI：工作流 `33303761813` 通过，共生成默认字体与 200% 字体各 12 张当前截图。
- 当前候选核心路径：工作区默认仅 SSH；AI CLI 新建/历史会话显式选择；tmux 页面无有效工作区时提供
  明确返回动作。
- 正式版合入前必须重新通过 JVM/Robolectric、Lint、构建、隔离 tmux 和模拟器 UI 门禁。
- 本地正式构建已通过 383 个 Gradle 任务（3 分 17 秒）；ARM64 APK SHA-256 为
  `f2528f47d8f98a8e783e34e982ff4b2755dc391f2d5552a009e29babccd3d8ee`。

## 风险匹配边界

- 已验证 Android 35 模拟器的 360dp、深色主题、简体中文与字体缩放，不将三星或小米单一品牌当作门禁。
- 未声称覆盖所有 OEM 后台限制、实体设备射频弱网、所有第三方输入法和 TalkBack 人工探索。
- 这些边界进入后续兼容性样本扩展，不构成当前范围内的 P0/P1；出现启动、SSH、数据或会话隔离回归时
  立即按 hotfix 流程处理。

# TermuxPro 0.4.0-rc.1 发布评审记录

## 当前结论

候选版状态：`READY TO PUBLISH`。远程 tmux 会话中心的功能、产品、UI/UX、安全和 QA 驳回项已关闭，
真实 SSH/tmux CI 与 Android 模拟器默认/200% 字体门禁通过。标签、正式签名 APK、SHA-256 和 Release
尚未生成，发布流水线成功前不得改写为 `PUBLISHED`。

稳定版状态：`HOLD`。候选发布不等于 `0.4.0` 稳定版 READY；API 21/28、真实手机弱网/后台恢复、
真实 Claude Code/Codex CLI 主旅程和候选范围 P0/P1 裁定仍需补齐。

## 冻结范围

- PR #42：远程 tmux 会话新建、进入、重命名和安全停止完整生命周期。
- PR #44、#45：自动 PR、同提交 CI/UI 门禁、自动合并及 merge commit 收尾 CI。

冻结后不再并入新的 P2 或普通功能；门禁失败只修复候选范围并递增候选编号。

## 已有证据

- 本地 `./scripts/test-all.sh`、Debug Lint 与 AndroidTest 编译通过。
- CI run `33359040097` 通过真实 OpenSSH/tmux fixture、全模块测试、Lint 与 Debug APK 构建。
- Emulator UI run `33358436301` 通过 360dp、深色主题、默认字体和 200% 字体截图门禁。
- PR #42 在最新提交的 CI/UI 全绿后合入 `dev`；PR #45 的 merge commit CI run `33365769374` 成功。
- 生产代码不存在默认 `tmux kill-server`；测试清理只使用唯一 `-L` 隔离服务器或单会话停止。

## 发布后待补证据

1. 标签 `v0.4.0-rc.1` 精确指向发布 PR 合入后的 `dev` 提交。
2. Release/R8 构建验证 `versionName=0.4.0-rc.1`、`versionCode=40001`、ARM64 ABI、v1/v2 签名和公开证书。
3. Release 非 Draft、标记为 Pre-release，APK、SHA-256、签名报告和发布说明来自同一提交。
4. 从 `0.2.0` 正式签名 APK覆盖升级到候选 APK，并验证 UID、Launcher、桌面名称、进程和致命崩溃。

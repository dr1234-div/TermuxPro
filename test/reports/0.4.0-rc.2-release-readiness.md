# TermuxPro 0.4.0-rc.2 发布评审记录

## 当前结论

候选版状态：`PUBLISHED`。`0.4.0-rc.2` 不新增应用功能，范围限定为 `0.4.0-rc.1` 的 tmux 会话中心
与发布流水线门禁修正。`dev` 发布提交 `fde55f2b` 已通过完整 CI、模拟器 UI、merge commit 收尾 CI、
发布前签名 APK 覆盖升级验收和 GitHub Pre-release 创建。

稳定版状态：`READY`。`0.4.0-rc.2` 的 Release 流水线已在公开 Release 创建前完成签名 APK 覆盖升级
验收，且当前冻结范围没有未关闭 P0/P1；允许推进 `dev → master` 和 `v0.4.0` 稳定发布。

## 冻结范围

- PR #42：远程 tmux 会话新建、进入、重命名和安全停止完整生命周期。
- PR #44、#45：自动 PR、同提交 CI/UI 门禁、自动合并及 merge commit 收尾 CI。
- PR #47、#48：Release 候选安装升级门禁、发布前验收顺序、研发 PR action_required 噪声治理和
  GitHub CLI 仓库默认值修正。

冻结后不再并入新的 P2 或普通功能；门禁失败只修复候选范围并递增候选编号。

## 已有证据

- 本地 `TERMUXPRO_USE_CHINA_MIRROR=1 ./scripts/test-all.sh` 成功，包含版本元数据、稳定基线选择、
  GitHub CLI 封装、workflow 触发策略、Release APK 冒烟脚本、Skill 校验和 Gradle 单元测试。
- PR #48 head `32d106a4` 的完整 CI run `33376862931` 成功。
- PR #48 head `32d106a4` 的 Emulator UI run `33376862962` 成功，覆盖默认字体与 200% 字体截图。
- PR #48 已合入 `dev`，merge commit `19443f3f`。
- `dev` merge commit CI run `33377467015` 成功。
- PR #49 head `8f91a48f` 的完整 CI run `33378207673` 成功。
- PR #49 head `8f91a48f` 的 Emulator UI run `33378207696` 成功。
- PR #49 已合入 `dev`，merge commit `fde55f2b`。
- `dev` merge commit CI run `33378758762` 成功。
- `v0.4.0-rc.2` Release run `33379101232` 成功，且“验收待发布签名 APK 的覆盖升级”先于“创建 GitHub Release”完成。
- GitHub Release `v0.4.0-rc.2` 非 Draft、标记为 Pre-release，附件包含 APK、`SHA256SUMS` 和 `APK_SIGNATURE.txt`。

## 发布证据

1. 标签 `v0.4.0-rc.2` 精确指向发布 PR 合入后的 `dev` 提交 `fde55f2b`。
2. Release/R8 构建验证 `versionName=0.4.0-rc.2`、`versionCode=40002`、ARM64 ABI、v1/v2 签名和公开证书。
3. Release 创建前完成 `0.2.0` 稳定版到候选 APK 的覆盖升级，验证 UID、Launcher、桌面名称、进程和
   AndroidRuntime 无致命崩溃。
4. Release 非 Draft、标记为 Pre-release，APK、SHA-256、签名报告和发布说明来自同一提交。

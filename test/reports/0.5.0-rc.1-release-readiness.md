# TermuxPro 0.5.0-rc.1 发布评审记录

## 当前结论

候选版状态：`READY_FOR_PR`。`dev` 自 `v0.4.0` 后已有多项用户可见体验改动，符合进入候选版冻结的条件。
冻结范围只包含首页入口、终端工具箱、tmux 会话列表上下文和设置页视觉语义修复；不继续并入 P2 或新功能。

## 冻结范围

- PR #55/#56：首页信息架构瘦身与工具箱入口显性化，以及对应验收证据。
- PR #57：tmux 会话列表展示创建/活跃时间，并修复会话名冒号解析隐患。
- PR #58：终端工具箱按开发者任务分组。
- PR #59：设置页改用 TermuxPro 产品主题，移除上游红色主色误导。

## 本地证据

- PR #57、#58、#59 均已通过本地 `./scripts/test-all.sh`、GitHub CI、Emulator UI 和 dev merge commit 收尾 CI。
- 候选冻结提交已通过本地
  `TERMUXPRO_USE_CHINA_MIRROR=1 ./scripts/test-all.sh && git diff --check`，包含版本元数据、稳定基线选择、
  GitHub CLI 封装、workflow 触发策略、Release APK 冒烟脚本、Skill 校验和 Gradle 全量单元测试。

## 发布门禁

1. `termuxpro-version.properties` 必须为 `versionName=0.5.0-rc.1`、`versionCode=50001`，高于 `0.4.0`。
2. 本地 `TERMUXPRO_USE_CHINA_MIRROR=1 ./scripts/test-all.sh && git diff --check` 必须通过。
3. release PR head 的完整 CI 与 Emulator UI 必须通过。
4. release PR 合入 dev 后，merge commit 收尾 CI 必须通过。
5. 标签 `v0.5.0-rc.1` 必须指向 release PR 合入后的 dev merge commit。
6. Release 工作流必须先完成待发布签名 APK 覆盖升级验收，再创建 GitHub Pre-release。
7. Release 必须非 Draft、标记为 Pre-release，并包含 APK、`SHA256SUMS` 和 `APK_SIGNATURE.txt`。

## 待补证据

- PR head CI / Emulator UI
- dev merge commit CI
- GitHub Pre-release 与 APK 附件校验
